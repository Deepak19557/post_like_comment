<%= form_with model: [ @comment ] do |f| %>
  <%= f.text_field :comment_name, placeholder: "What do you want to comment?" %><br><br>
  <%= f.hidden_field :post_id, :value => "#{@post.id}" %>
  <%= f.submit "Comment" %>
<% end %>
